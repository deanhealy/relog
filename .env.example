# Server-side only — never prefixed with NEXT_PUBLIC_

# TMDB (themoviedb.org) — v3 API key
TMDB_API_KEY=

# RAWG (rawg.io) — API key
RAWG_API_KEY=

# Google Books (console.cloud.google.com) — API key (optional but recommended)
GOOGLE_BOOKS_API_KEY=
